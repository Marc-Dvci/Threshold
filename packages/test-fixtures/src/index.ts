/**
 * @threshold/test-fixtures
 *
 * Seeded inventories and named scenarios, shared by the providers, the tests, the eval suite and
 * the recording rig. One definition of "the golden scenario" so those four cannot drift into
 * describing different things.
 */

export * from './inventories';
export * from './scenarios';
