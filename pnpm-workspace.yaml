packages:
  - 'packages/*'
  - 'apps/*'

# esbuild ships a platform binary through a postinstall script. pnpm 10 refuses to run any install
# script unless it is named here, which is the right default and means naming this one.
onlyBuiltDependencies:
  - esbuild
